To run the project PLease spin a SQL Server db (docker/local/server) either of it works.
You can place the connection string in the appsetttings.json file under DefaultConnection tag.
The project is compiled using dotnet 9.0.301 and Entity Framework 6.0.27
The code is using default port http://localhost:5156, so make sure to have the ports free for execution.

